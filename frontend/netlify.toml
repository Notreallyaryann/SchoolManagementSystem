[build]
  publish = "frontend/build"
  command = "npm run build"

[build.environment]
  NPM_FLAGS = "--verbose"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

